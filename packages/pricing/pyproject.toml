[project]
name = "rokkam-pricing"
version = "0.1.0"
description = "Rokkam quote engine: base - deductions + bonuses, scrap floor, kills_deal"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["rokkam_pricing*"]
